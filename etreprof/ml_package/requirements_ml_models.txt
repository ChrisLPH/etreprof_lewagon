# Requirements pour les modèles ML (VERSION MOCK)
# À ajuster selon les vrais besoins quand les modèles de l'équipe seront prêts
# Généré par IA pour simuler le comportement en attendant l'intégration de modèles réels.
# A utiliser uniquement pour les tests et la démonstration de l'interface utilisateur.
# Ne pas utiliser en production.

# Core ML & Data Science
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.3.0

# Pour l'éventuel traitement NLP (si nécessaire)
nltk>=3.8
# transformers>=4.20.0  # Décommenter si besoin de modèles transformers

# Visualisation (optionnel pour les analyses)
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilitaires
python-dotenv>=0.19.0
tqdm>=4.64.0

# Tests
pytest>=7.0.0
pytest-cov>=4.0.0

# Pour le packaging des modèles
joblib>=1.2.0
pickle5>=0.0.11

# API et déploiement (sera dans requirements.txt principal)
# fastapi>=0.95.0
# streamlit>=1.28.0
